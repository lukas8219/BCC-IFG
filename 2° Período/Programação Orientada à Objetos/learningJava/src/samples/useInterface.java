package samples;

public class useInterface {
	//Notice that the methods that are declared have no bodies. They enthe parameterd with a semicolon after
	// list; they are, essentially, abstract methods. Each class that includes such a interface must implement all
	// of the methods. As the general form shows, variables can be declared inside of interface declarations. They
	// are implicitly final, static and public
	// pag 239
	
}
